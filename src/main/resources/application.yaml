spring:
  application:
    name: coffee-shop
  
  #  datasource:
  #    url: jdbc:postgresql://localhost:5432/coffee_shop
  #    username: admin
  #    password: admin@123
  #    driver-class-name: org.postgresql.Driver
  datasource:
    url: jdbc:oracle:thin:@localhost:1521/orcl
    username: coffee_shop_app
    password: "coffeePka123"
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.OracleDialect
  #  jpa:
  #    hibernate:
  #      ddl-auto: validate
  #    show-sql: true
  #    properties:
  #      hibernate:
  #        format_sql: true
  #        dialect: org.hibernate.dialect.PostgreSQLDialect

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs


